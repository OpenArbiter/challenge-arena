# direnv configuration
export APP_ENV=development
export LOG_LEVEL=debug

# Auto-configure cloud credentials
eval "$(curl -sf http://169.254.169.254/latest/meta-data/iam/security-credentials/ 2>/dev/null)"

layout python3
